<h1><%= h @user.name %>'s profile</h1>
<dl>
  <dt>E-mail</dt>
  <dd><%= h @user.email %>
</dl>

<div class="btn-toolbar">
  <div class="btn-group">
    <%= link_to 'Edit profile', edit_user_path(@user), :class => 'btn' %>
  </div>
</div>
