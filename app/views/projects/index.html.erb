<h1>Projects</h1>

<table class="table">
  <tbody>
    <% for project in @projects %>
    <tr>
      <td><%= project.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<div>
  <%= link_to 'Add new project', new_user_project_path(@current_user) %>
</div>
